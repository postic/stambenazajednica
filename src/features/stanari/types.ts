export interface Stanar {
  id: string;
  title: string;
  body: string;
  created: string;
  telefon: string;
  email: string;
  status: string;
  statusName?: string; // 👈 DODAJ ?
  image?: string;
}
